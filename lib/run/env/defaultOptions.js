module.exports = {
    // base路径
    BASE: process.cwd(),
    // env文件夹路径
    DIR_PATH: 'env',
    ENV_FILE_PATH: '.env',
    FILE_MATCH_REG: /^\.env\.(.*)$/,
    OUTPUT: false,
    OUTPUT_PATH: 'XY-ENV.js',
    MODE: ''
}